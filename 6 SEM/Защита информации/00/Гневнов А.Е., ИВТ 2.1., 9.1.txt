> #
> #  
> a := 12
> z := solve(a * (x * x - 1) = sqrt(1 + x / a), x);
> z := min(z):
> z := abs(z):
> z := evalf(z);
> # пароль должен содержать 6 цифр
> # если меньше нужно дополнить 0
> while z/10^5 < 1 do
> z := z * 10:
> end do:
> z := floor(z);
> 
> 