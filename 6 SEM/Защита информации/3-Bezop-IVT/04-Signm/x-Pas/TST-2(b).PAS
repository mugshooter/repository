   PROGRAM Test;
   { Изменённый комментарий к Вашей программе }
      Uses CRT;
      {$i MyCom.txt} { Эта строчка добавляется при контроле }
   BEGIN
      Copyright;     { Эта строчка добавляется при контроле }
      WriteLn;       { Эта строчка добавляется при контроле }
      WriteLn;       { Эта строчка добавляется при контроле }
      WriteLn('Моя собственная программа...');
      ReadKey
   END.
