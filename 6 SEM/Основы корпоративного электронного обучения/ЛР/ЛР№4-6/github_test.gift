// Тест "Работа в GitHub" - 20 вопросов

::Что такое Git?::Что такое Git?{
=Система контроля версий
~Облачное хранилище кода
~Графический редактор
~Социальная сеть для программистов
}

::Создание репозитория::Как создать новый репозиторий на GitHub?{
=Нажать "+" → "New repository"
~Использовать команду `git create repo`
~Через команду `github --new`
~В настройках профиля
}

::Команда clone::Что делает команда `git clone`?{
=Копирует удалённый репозиторий на ПК
~Удаляет локальную копию
~Создает ветку
~Фиксирует изменения
}

::Добавление файлов::Как добавить все измененные файлы в зону подготовки (staging)?{
=`git add .`
~`git add -file`
~`git commit -a`
~`git push --all`
}

::Pull Request::Что такое Pull Request (PR)?{
=Предложение слить изменения в чужой проект
~Запрос на удаление репозитория
~Инструмент для отладки
~Система уведомлений GitHub
}

::Отправка изменений::Какой командой отправить изменения на GitHub?{
=`git push`
~`git send`
~`git save`
~`git commit`
}

::Файл .gitignore::Для чего используется файл `.gitignore`?{
=Исключение файлов из отслеживания
~Хранение паролей
~Настройка интерфейса Git
~Автоматические коммиты
}

::Команда pull::Что делает команда `git pull`?{
=Загружает изменения с удалённого репозитория
~Удаляет последний коммит
~Создает резервную копию
~Показывает историю изменений
}

::Создание ветки::Как создать новую ветку и сразу переключиться на неё?{
=`git checkout -b new-feature`
~`git branch new-feature`
~`git switch new-feature`
~`git create branch new-feature`
}

::Fork::Что такое Fork репозитория?{
=Копия чужого репозитория в ваш аккаунт
~Удаление ветки
~Инструмент для слияния конфликтов
~Система тегов
}

::История коммитов::Как просмотреть историю коммитов?{
=`git log`
~`git show`
~`git history`
~`git list`
}

::Конфликты слияния::Где разрешаются конфликты слияния?{
=Вручную в коде (markers <<< HEAD)
~В файле `README.md`
~Через GitHub Desktop
~Автоматически при `git push`
}

::Отмена коммита::Что делает команда `git reset --hard HEAD~1`?{
=Отменяет последний коммит (без сохранения изменений)
~Создает резервную копию
~Переименовывает ветку
~Синхронизирует с GitHub
}

::Issues::Для чего нужны Issues в GitHub?{
=Обсуждение багов и задач
~Хранение релизов
~Настройка CI/CD
~Управление ветками
}

::Привязка репозитория::Как привязать локальный репозиторий к удалённому на GitHub?{
=`git remote add origin [url]`
~`git link origin`
~`github connect`
~`git attach [url]`
}

::GitHub Actions::Что такое GitHub Actions?{
=Инструмент CI/CD
~Чат для разработчиков
~Система видеоконференций
~Marketplace плагинов
}

::Просмотр изменений::Какой командой просмотреть изменения до коммита?{
=`git diff`
~`git preview`
~`git changes`
~`git status --details`
}

::Merge conflict::Что означает статус "Merge conflict"?{
=Git не может автоматически объединить изменения
~Успешное слияние
~Ошибка сети
~Требуется подтверждение PR
}

::Настройки Git::Где хранятся настройки Git?{
=В `.gitconfig` (пользовательские) и `.git/config` (локальные)
~В файле `github.cfg`
~Только в облаке GitHub
~В системном реестре
}

::Публикация ветки::Как опубликовать ветку на GitHub после создания?{
=`git push -u origin branch-name`
~Автоматически при коммите
~`git publish branch`
~Через Pull Request
}