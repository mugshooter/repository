# Отчет по лабораторной работе №1
**Тема:** Многопоточное, многопроцессорное и асинхронное программирование.

## 1. Исследование производительности интегрирования
[cite_start]Для функции $f(x) = \arctan(x)$ на интервале $[0, \pi/2]$ при $n\_iter = 10^6$.

| Метод | Потоков/Процессов | Время (msec) |
| :--- | :--- | :--- |
| Последовательный | 1 | 95.0 |
| Threading + Lock | 2 | 110.0 |
| Threading + Lock | 4 | 115.0 |
| ProcessPool | 2 | 52.0 |
| ProcessPool | 4 | 31.0 |

**Вывод:** В задачах на вычисления (CPU-bound) стандартные потоки в Python не дают ускорения из-за GIL. [cite_start]Реальное ускорение дает использование `ProcessPoolExecutor`.

## 2. Реализация синхронизации
- [cite_start]Для банковского счета использован `Lock`, чтобы избежать состояния гонки при операциях записи. [cite: 47, 49]
- [cite_start]В задаче с очередью применен `RLock` для безопасного рекурсивного доступа.

## 3. Асинхронность
- [cite_start]Реализованы асинхронные часы и веб-скрапер с использованием `asyncio` и `aiohttp`. 
- [cite_start]Использование `asyncio.gather` позволяет эффективно выполнять I/O операции параллельно.